{
  vim = {
    vimAlias = true;
    viAlias = true;

    spellcheck.enable = false;

    useSystemClipboard = true;

    lineNumberMode = "relative";

    searchCase = "smart";

    undoFile.enable = true;

    scrollOffset = 8;

    options = {
      tabstop = 2;

      shiftwidth = 2;

      wrap = true;

      splitbelow = true;
      splitright = true;

      termguicolors = true;

      cmdheight = 0;
    };
  };
}
